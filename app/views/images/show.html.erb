<%= render @image %>

<div class="mt-3">
  <%= link_to "Edit", edit_image_path(@image), class: "link-underline-light" %> |
  <%= link_to "Back to images", images_path, class: "link-underline-light" %>

  <div class="mt-3">
    <%= link_to "Delete", @image, data: { turbo_method: :delete }, class: "btn btn-outline-primary" %>
  </div>
</div>
